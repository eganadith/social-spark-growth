[build]
  command = "bun run build"
  publish = "dist"

# Vite embeds VITE_* in the browser bundle; Supabase anon + URL are public by design (RLS enforces access).
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_URL,VITE_SUPABASE_ANON_KEY"
